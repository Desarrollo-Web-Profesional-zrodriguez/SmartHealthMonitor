<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Declarar que es una app de TV -->
    <uses-feature android:name="android.software.leanback"
        android:required="true" />
    <!-- No requiere pantalla táctil -->
    <uses-feature android:name="android.hardware.touchscreen"
        android:required="false" />

    <application
        android:name=".SmartHealthTVApp"
        android:allowBackup="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartHealthTV"
        android:banner="@drawable/banner_tv"
        android:icon="@drawable/banner_tv"> <!-- Usando el banner como icono temporal -->
        <activity android:name=".TVActivity"
            android:exported="true"
            android:label="SmartHealth TV">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <!-- Categoría LEANBACK_LAUNCHER: aparece en launcher TV -->
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name="mx.utng.smarthealthmonitor.tv.TVWearListenerService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.DATA_CHANGED" />
                <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED" />
                <data android:scheme="wear" android:host="*" android:pathPrefix="/smarthealth" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED" />
                <data android:scheme="wear" android:host="*" android:pathPrefix="/smarthealthmonitor" />
            </intent-filter>
        </service>

        <meta-data
            android:name="com.google.android.gms.wearable.capabilities"
            android:resource="@xml/wearable_app_desc" />
    </application>
</manifest>